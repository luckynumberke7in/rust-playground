// variables4.rs
// Execute `rustlings hint variables4` or use the `hint` watch subcommand for a hint.

fn main() {
    let mut x = 3;
    println!("This exercise checks a mutable variable: {}", x);
    x = 5; // don't change this line
    println!("Then checks the same variable after mutation: {}", x);
}
