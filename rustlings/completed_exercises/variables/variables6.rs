// variables6.rs
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a hint.

const NUMBER: u8 = 3; // const declarations need an explicit type declarations and to be in all CAPS
fn main() {
    println!("Number {}", NUMBER);
}
